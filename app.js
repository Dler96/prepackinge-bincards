const form=document.querySelector('#prepackForm'),error=document.querySelector('#formError'),success=document.querySelector('#success');
form.addEventListener('submit',event=>{event.preventDefault();error.textContent='';const required=[...form.querySelectorAll('[required]')];let firstInvalid=null;required.forEach(field=>{field.removeAttribute('aria-invalid');if(!field.value.trim()){field.setAttribute('aria-invalid','true');if(!firstInvalid)firstInvalid=field}});if(firstInvalid){error.textContent='Please complete all required fields.';firstInvalid.focus();return}const submit=form.querySelector('.submit');submit.disabled=true;submit.querySelector('span').textContent='Sending…';form.submit();setTimeout(()=>{form.hidden=true;success.hidden=false;submit.disabled=false;submit.querySelector('span').textContent='Submit request'},700)});
document.querySelector('#newRequest').addEventListener('click',()=>{form.reset();form.hidden=false;success.hidden=true;form.querySelector('#drug').focus()});
if(document.modelContext?.registerTool){try{Promise.resolve(document.modelContext.registerTool({name:'validate_prepacking_request',title:'Validate prepacking request',description:'Check whether a prepacking form is complete before submission.',inputSchema:{type:'object',properties:{drug:{type:'string'},location:{type:'string'},packs:{type:'string'},name:{type:'string'}},required:['drug','location','packs','name'],additionalProperties:false},annotations:{readOnlyHint:true},execute(input){if(!input||['drug','location','packs','name'].some(k=>typeof input[k]!=='string'||!input[k].trim()))throw new Error('All fields are required');return {valid:true}}})).catch(()=>{})}catch{}}
